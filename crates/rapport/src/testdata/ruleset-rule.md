# rapport ruleset show --rule

- `id` — CODE_001
- `owning ruleset` — CODE
- `reference` — none

Use explicit names.

## Rationale

Names retain intent.

## Avoid

```rust
let x = value;
```

## Prefer

```rust
let person_count = value;
```
