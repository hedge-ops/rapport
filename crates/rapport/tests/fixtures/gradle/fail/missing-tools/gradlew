#!/bin/sh
set -u

if ! command -v java >/dev/null 2>&1; then
    echo "ERROR: JAVA_HOME is not set and no 'java' command could be found in your PATH." >&2
    exit 1
fi

echo "missing-tools fixture should fail during Java resolution" >&2
exit 2
